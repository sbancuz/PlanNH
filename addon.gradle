// Applied automatically by the GTNH convention plugin after the main buildscript.

test {
    useJUnitPlatform()
    testLogging {
        events 'passed', 'skipped', 'failed'
        exceptionFormat 'short'
    }
}
